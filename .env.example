# Database Configuration
DATABASE_CONNECTION_STRING=Server=localhost;Database=RoomBookingDB;Trusted_Connection=True;

# JWT Configuration (jika pakai auth)
JWT_SECRET=your-secret-key-here
JWT_ISSUER=RoomBookingAPI
JWT_AUDIENCE=RoomBookingClient

# API Settings
API_PORT=5000